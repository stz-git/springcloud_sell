spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev
    stream:
      bindings:
        output:
          group: order
          contentType: application/json
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
