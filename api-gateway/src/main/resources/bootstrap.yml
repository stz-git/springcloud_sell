spring:
  application:
    name: api-gateway
#  cloud:
#    config:
#      discovery:
#        service-id: config
#        enabled: true
#      profile: dev
  redis:
    host: 192.168.13.253
    port: 6379
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
zuul:
  sensitive-headers:
server:
  port: 8085